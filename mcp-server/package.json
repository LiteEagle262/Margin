{
  "name": "margin-mcp-server",
  "version": "1.0.0",
  "description": "MCP server that exposes Margin browser automation tools to Claude Code, Codex, Cursor, and other MCP clients.",
  "type": "module",
  "main": "index.js",
  "bin": {
    "margin-mcp": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check temp-email.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "ws": "^8.18.2"
  }
}
