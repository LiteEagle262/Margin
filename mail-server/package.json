{
  "name": "scrapeflow-mail-server",
  "version": "1.0.0",
  "description": "Disposable inbox backend for ScrapeFlow. Receives mail via SMTP or webhook, exposes a REST API used by the MCP server so an AI agent can autonomously sign up to and verify accounts on third-party sites.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mailparser": "^3.7.1",
    "smtp-server": "^3.13.4"
  }
}
